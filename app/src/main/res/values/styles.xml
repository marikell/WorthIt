<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorAccent</item>
        <item name="colorPrimaryDark">@color/colorAccent</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:fontFamily">@font/opensanscondlight</item>
        <item name="fontFamily">@font/opensanscondlight</item> <!-- target android sdk versions < 26 and > 14 if theme other than AppCompat -->
    </style>

    <style name="WorthItTheme" parent="Theme.AppCompat.Light.NoActionBar">

        <item name="windowNoTitle">true</item>


    </style>

    <style name="optionText">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="HomeSearchView">
        <item name="android:textSize">15dp</item>
    </style>

    <style name="WorthIt" parent="Theme.AppCompat.Light" >
        <item name="colorPrimary">@color/colorAccent</item>
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="android:colorForeground">@color/colorText</item>
        <item name="android:navigationBarColor">@color/colorText</item>
        <item name="android:statusBarColor">@color/colorText</item>
        <item name="windowActionBar">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="toolbarStyle">@style/WorthItActionBar</item>
        <item name="coordinatorLayoutStyle">@style/Widget.Design.CoordinatorLayout</item>
    </style>


    <style name="CustomRatingBar" parent="Widget.AppCompat.RatingBar.Small">
        <item name="colorAccent">@color/rating_bar</item>
        <item name="android:colorControlNormal">@color/colorText</item>
    </style>


    <style name="primaryButton">
        <item name="android:textColor">@color/colorBackground</item>
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:textSize">23dp</item>
        <item name="android:fontFamily">@font/opensanscondbold</item>
    </style>

    <style name="secondaryButton">
        <item name="android:textColor">@color/colorBackground</item>
        <item name="android:background">@color/colorAccent</item>
        <item name="android:textSize">23dp</item>
        <item name="android:fontFamily">@font/opensanscondbold</item>
    </style>

    <!-- Primeira Barra-->
    <style name="WorthItActionBar" parent="@android:style/Widget.Toolbar">
        <item name="android:background">@color/colorPrimary</item>
    </style>
</resources>
